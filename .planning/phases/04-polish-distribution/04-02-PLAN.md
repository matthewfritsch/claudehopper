---
phase: 04-polish-distribution
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .goreleaser.yaml
  - .github/workflows/release.yml
autonomous: true
requirements:
  - DIST-01
  - DIST-03

must_haves:
  truths:
    - "goreleaser config is valid and produces both hop and claudehopper binaries for linux/darwin on amd64/arm64"
    - "Shell completions output valid scripts for bash, zsh, fish, and powershell"
    - "A GitHub Actions workflow triggers goreleaser on version tags"
    - "Homebrew tap configuration exists in goreleaser for automated formula publishing"
  artifacts:
    - path: ".goreleaser.yaml"
      provides: "Complete release configuration with homebrew_casks"
      contains: "homebrew_casks"
    - path: ".github/workflows/release.yml"
      provides: "GitHub Actions release workflow"
      contains: "goreleaser"
  key_links:
    - from: ".github/workflows/release.yml"
      to: ".goreleaser.yaml"
      via: "goreleaser-action invokes goreleaser with this config"
      pattern: "goreleaser/goreleaser-action"
    - from: ".goreleaser.yaml"
      to: "homebrew-claudehopper repo"
      via: "homebrew_casks block pushes formula to tap"
      pattern: "homebrew_casks"
---

<objective>
Finalize goreleaser configuration with Homebrew tap, create GitHub Actions release workflow, and verify shell completions.

Purpose: The tool needs to be distributable via GitHub Releases, Homebrew, and `go install`. Shell completions must work out of the box.
Output: Updated `.goreleaser.yaml` with homebrew_casks, `.github/workflows/release.yml`, verified shell completions.
</objective>

<execution_context>
@/home/matthew/.claude/get-shit-done/workflows/execute-plan.md
@/home/matthew/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-polish-distribution/04-RESEARCH.md

@.goreleaser.yaml
@main.go

<interfaces>
<!-- Existing goreleaser config structure -->

From .goreleaser.yaml:
```yaml
version: 2
builds:
  - id: claudehopper  # dual build already exists
  - id: hop           # dual build already exists
archives:
  - id: combined      # both binaries in one archive
checksum:
  name_template: checksums.txt
```

From main.go:
```go
var version = "dev"   # ldflags: -X main.version={{.Version}}
var commit = "none"   # ldflags: -X main.commit={{.ShortCommit}}
var date = "unknown"  # ldflags: -X main.date={{.Date}}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add homebrew_casks to goreleaser and create release workflow</name>
  <files>.goreleaser.yaml, .github/workflows/release.yml</files>
  <action>
    1. Update `.goreleaser.yaml` — append `homebrew_casks` block after the existing `changelog` section:
       ```yaml
       homebrew_casks:
         - repository:
             owner: matthewfritsch
             name: homebrew-claudehopper
             token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
           homepage: "https://github.com/matthewfritsch/claudehopper"
           description: "Instant, safe profile switching for Claude Code configs"
           license: MIT
           binaries:
             - hop
             - claudehopper
       ```
       Do NOT change any existing config (builds, archives, checksum, changelog sections stay as-is).

    2. Create `.github/workflows/release.yml`:
       ```yaml
       name: goreleaser
       on:
         push:
           tags:
             - "v*"
       permissions:
         contents: write
       jobs:
         goreleaser:
           runs-on: ubuntu-latest
           steps:
             - uses: actions/checkout@v4
               with:
                 fetch-depth: 0
             - uses: actions/setup-go@v5
               with:
                 go-version: stable
             - uses: goreleaser/goreleaser-action@v7
               with:
                 distribution: goreleaser
                 version: "~> v2"
                 args: release --clean
               env:
                 GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
                 HOMEBREW_TAP_GITHUB_TOKEN: ${{ secrets.HOMEBREW_TAP_GITHUB_TOKEN }}
       ```
       Create `.github/workflows/` directory if it does not exist.

    3. Validate with `goreleaser check` if goreleaser is installed locally. If not installed, note this in the summary as a manual verification step. The config structure follows goreleaser v2 docs exactly.
  </action>
  <verify>
    <automated>cd /home/matthew/Programming/claudehopper-go && cat .goreleaser.yaml | grep -c "homebrew_casks" && test -f .github/workflows/release.yml && echo "PASS"</automated>
  </verify>
  <done>.goreleaser.yaml contains homebrew_casks block for matthewfritsch/homebrew-claudehopper. .github/workflows/release.yml exists with goreleaser-action triggered on v* tags. Both files have correct structure.</done>
</task>

<task type="auto">
  <name>Task 2: Verify shell completions output valid scripts</name>
  <files></files>
  <action>
    1. Build the binary: `go build -o /tmp/hop-test .`

    2. Verify each shell completion generates output (Cobra auto-generates these, so this is verification not implementation):
       - `/tmp/hop-test completion bash` — should output a bash completion script. Validate with `bash -n` (syntax check).
       - `/tmp/hop-test completion zsh` — should output a zsh completion script. Verify it contains `#compdef` header.
       - `/tmp/hop-test completion fish` — should output a fish completion script. Verify it contains `complete -c`.
       - `/tmp/hop-test completion powershell` — should output a PowerShell completion script. Verify it contains `Register-ArgumentCompleter`.

    3. Verify that `completion` appears in `--help` output: `/tmp/hop-test --help | grep completion`.

    4. Clean up: `rm /tmp/hop-test`.

    5. If any completion fails to generate or has syntax errors, investigate the Cobra command tree for issues (missing command descriptions, invalid characters). Fix in the relevant cmd/*.go file if needed.

    Note: This task modifies no files if completions work (expected case since Cobra handles this). Only modifies cmd files if a bug is found.
  </action>
  <verify>
    <automated>cd /home/matthew/Programming/claudehopper-go && go build -o /tmp/hop-test . && /tmp/hop-test completion bash | bash -n && /tmp/hop-test completion zsh | head -1 | grep -q compdef && /tmp/hop-test completion fish | grep -q "complete -c" && /tmp/hop-test completion powershell | grep -q "Register-ArgumentCompleter" && rm /tmp/hop-test && echo "ALL COMPLETIONS VALID"</automated>
  </verify>
  <done>All four shell completion outputs (bash, zsh, fish, powershell) generate valid scripts. bash completion passes syntax check. zsh has #compdef header. fish has complete -c directives. PowerShell has Register-ArgumentCompleter.</done>
</task>

</tasks>

<verification>
- `.goreleaser.yaml` has homebrew_casks section with correct repository owner/name
- `.github/workflows/release.yml` exists with correct trigger, permissions, and steps
- All four shell completions generate valid output
- `go build ./...` still compiles
- `go test -v -race ./...` all tests pass
</verification>

<success_criteria>
- goreleaser config includes homebrew_casks for matthewfritsch/homebrew-claudehopper
- GitHub Actions release workflow triggers on v* tags with fetch-depth: 0
- Workflow passes GITHUB_TOKEN and HOMEBREW_TAP_GITHUB_TOKEN to goreleaser
- bash completion passes bash -n syntax validation
- zsh, fish, powershell completions contain expected markers
- No existing functionality broken
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish-distribution/04-02-SUMMARY.md`
</output>
