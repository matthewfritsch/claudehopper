---
phase: 04-polish-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/updater/updater.go
  - internal/updater/updater_test.go
  - cmd/update.go
  - cmd/status.go
  - go.mod
  - go.sum
autonomous: true
requirements:
  - OPS-02

must_haves:
  truths:
    - "hop status shows update notice when a newer GitHub release exists and TTL has expired"
    - "hop status does not slow down when GitHub is unreachable (3s timeout)"
    - "hop update downloads and replaces the binary (binary install) or runs go install (source install)"
    - "Update check is skipped when stamp file is less than 24h old"
  artifacts:
    - path: "internal/updater/updater.go"
      provides: "CheckForUpdate and PerformUpdate functions"
      exports: ["CheckForUpdate", "PerformUpdate"]
    - path: "internal/updater/updater_test.go"
      provides: "Unit tests for TTL cache and version comparison"
      contains: "TestCheckForUpdate"
    - path: "cmd/update.go"
      provides: "hop update command"
      contains: "updateCmd"
  key_links:
    - from: "cmd/status.go"
      to: "internal/updater"
      via: "goroutine call to CheckForUpdate after status display"
      pattern: "updater\\.CheckForUpdate"
    - from: "cmd/update.go"
      to: "internal/updater"
      via: "PerformUpdate call"
      pattern: "updater\\.PerformUpdate"
---

<objective>
Implement update checking with 24h cached TTL and the `hop update` command.

Purpose: Users need to know when a newer version is available and be able to upgrade in-place without manual download.
Output: `internal/updater/` package, `cmd/update.go` command, wired into `cmd/status.go` with non-blocking goroutine check.
</objective>

<execution_context>
@/home/matthew/.claude/get-shit-done/workflows/execute-plan.md
@/home/matthew/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-polish-distribution/04-RESEARCH.md

@cmd/status.go
@cmd/root.go
@cmd/helpers.go
@main.go
@go.mod
@internal/config/config.go

<interfaces>
<!-- Existing interfaces the executor needs -->

From main.go:
```go
var version = "dev"
var commit = "none"
var date = "unknown"
```

From cmd/root.go:
```go
func SetVersionInfo(version, commit, date string)
func Execute() error
var rootCmd = &cobra.Command{...}
```

From cmd/helpers.go:
```go
func isInteractive() bool
func claudeDir() string
```

From internal/config/config.go:
```go
func ConfigDir() (string, error)  // returns ~/.config/claudehopper or XDG override
func ConfigFilePath() (string, error)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create internal/updater package with TTL-cached update check</name>
  <files>internal/updater/updater.go, internal/updater/updater_test.go, go.mod, go.sum</files>
  <behavior>
    - TestCheckForUpdate_SkipsWithinTTL: When stamp file is less than 24h old, returns nil without making any network call
    - TestCheckForUpdate_CallsAPIAfterTTL: When stamp file is older than 24h (or missing), calls detect and returns release info if newer version exists
    - TestCheckForUpdate_AlreadyLatest: When latest version equals current version, returns nil
    - TestCheckForUpdate_WritesStampAfterCheck: After a check (success or failure), stamp file is written/updated
    - TestCheckForUpdate_StampMissing: When no stamp file exists, proceeds with check (first-run case)
  </behavior>
  <action>
    1. Run `go get github.com/creativeprojects/go-selfupdate@v1.5.2` to add the dependency.

    2. Create `internal/updater/updater.go` with:
       - `CheckForUpdate(ctx context.Context, configDir, currentVersion string) (*UpdateInfo, error)` — reads stamp file at `filepath.Join(configDir, "update-check.stamp")`, returns nil if stamp ModTime is within 24h. Otherwise calls `selfupdate.DetectLatest(ctx, selfupdate.ParseSlug("matthewfritsch/claudehopper"))`. Writes stamp file after check regardless of result. Returns `&UpdateInfo{Version: latest.Version(), URL: ...}` if newer version found, nil otherwise. Strip leading "v" from currentVersion before comparison if needed.
       - `UpdateInfo` struct: `Version string`, `ReleaseURL string`
       - `PerformUpdate(ctx context.Context, currentVersion string) error` — creates `selfupdate.NewUpdater` with `Config{Validator: &selfupdate.ChecksumValidator{UniqueFilename: "checksums.txt"}}`. Detects latest, then checks if executable is in GOPATH/bin (source install) vs elsewhere (binary install). For source install: runs `exec.Command("go", "install", "github.com/matthewfritsch/claudehopper@v"+latest.Version())`. For binary install: calls `updater.UpdateTo(ctx, latest, exe)`. Prints result.
       - Helper `isSourceInstall() bool` — checks if `selfupdate.ExecutablePath()` is under `go env GOPATH`/bin.
       - Constants: `ttlDuration = 24 * time.Hour`, `stampFileName = "update-check.stamp"`, `repoSlug = "matthewfritsch/claudehopper"`.

    3. Create `internal/updater/updater_test.go`:
       - Tests use `t.TempDir()` for configDir isolation.
       - For TTL tests: create a stamp file with recent or old ModTime using `os.Chtimes`.
       - For API tests: the tests should verify the TTL gating logic without actually calling GitHub. Test the stamp file read/write and time comparison directly. Use a helper that accepts a `detectFunc` parameter or test the TTL portion separately from the network portion.
       - Note: go-selfupdate does not expose an easy mock interface. Focus tests on TTL stamp logic (file exists, file age, file creation) which is the hand-rolled portion. The go-selfupdate calls are thin wrappers around a well-tested library.

    4. Run `go test ./internal/updater/... -v -race` to verify.
  </action>
  <verify>
    <automated>cd /home/matthew/Programming/claudehopper-go && go test ./internal/updater/... -v -race</automated>
  </verify>
  <done>internal/updater package exists with CheckForUpdate and PerformUpdate exported. TTL stamp logic is tested: skip within 24h, proceed after 24h, create stamp on first run. All tests pass with -race.</done>
</task>

<task type="auto">
  <name>Task 2: Wire update check into cmd/status.go and create cmd/update.go</name>
  <files>cmd/update.go, cmd/status.go, main.go</files>
  <action>
    1. Create `cmd/update.go`:
       - `updateCmd` cobra.Command with Use: "update", Short: "Update claudehopper to the latest version".
       - `runUpdate` calls `updater.PerformUpdate(context.Background(), version)`.
       - `version` variable: add a package-level `var version string` in cmd package. Update `SetVersionInfo` in `root.go` to also store the bare version string (without commit/date) in this variable so `update.go` and `status.go` can access it.
       - Register in `init()`: `rootCmd.AddCommand(updateCmd)`.

    2. Modify `cmd/root.go`:
       - Add `var Version string` (exported) alongside existing `rootCmd`.
       - In `SetVersionInfo`, set `Version = version` (the bare version param, not the formatted string).

    3. Modify `cmd/status.go`:
       - After `fmt.Print(profile.FormatProfileStatus(info))`, add a non-blocking update check:
       - Get configDir via `config.ConfigDir()` (ignore error — degrade silently per project convention).
       - Launch goroutine calling `updater.CheckForUpdate(context.Background(), configDir, Version)`.
       - Use `select` with `case res := <-ch` and `case <-time.After(3 * time.Second)` for timeout.
       - If update available, print: `fmt.Fprintf(os.Stderr, "\nUpdate available: %s -> run 'hop update' to install\n", res.Version)`.
       - Import `context`, `time`, `os`, and `internal/updater`.

    4. Run full test suite to verify nothing breaks.
  </action>
  <verify>
    <automated>cd /home/matthew/Programming/claudehopper-go && go build ./... && go test -v -race ./...</automated>
  </verify>
  <done>`hop update` command exists and runs. `hop status` shows non-blocking update notice after profile status when a newer version is available. Full test suite passes. `hop update --help` prints usage.</done>
</task>

</tasks>

<verification>
- `go build ./...` compiles without errors
- `go test -v -race ./...` all tests pass including new updater tests
- `hop update --help` prints usage text
- `hop status --help` still works
- TTL stamp file is created/read from config directory
</verification>

<success_criteria>
- CheckForUpdate returns nil within 24h TTL window (tested)
- CheckForUpdate proceeds when stamp is stale or missing (tested)
- PerformUpdate detects source vs binary install and uses appropriate strategy
- hop status displays update notice non-blockingly with 3s timeout
- hop update command registered and callable
- No existing tests broken
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish-distribution/04-01-SUMMARY.md`
</output>
