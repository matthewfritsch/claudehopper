---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - internal/fs/atomic.go
  - internal/fs/atomic_test.go
  - internal/fs/protected.go
  - internal/fs/protected_test.go
  - internal/fs/testdata/python_shared_paths.txt
autonomous: true
requirements: [SAFE-01]

must_haves:
  truths:
    - "AtomicSymlink creates a new symlink that points to the intended target"
    - "AtomicSymlink replaces an existing symlink without ever leaving a broken state"
    - "IsProtected returns true for all 11 SHARED_PATHS entries"
    - "IsProtected returns false for paths not in the protected set"
    - "Go protected paths set matches SHARED_PATHS exactly (no drift)"
  artifacts:
    - path: "internal/fs/atomic.go"
      provides: "AtomicSymlink function wrapping renameio"
      exports: ["AtomicSymlink"]
      min_lines: 10
    - path: "internal/fs/protected.go"
      provides: "IsProtected function and sharedPaths constant set"
      exports: ["IsProtected"]
      min_lines: 20
    - path: "internal/fs/testdata/python_shared_paths.txt"
      provides: "Fixture of Python SHARED_PATHS for drift detection"
      min_lines: 11
  key_links:
    - from: "internal/fs/atomic.go"
      to: "github.com/google/renameio/v2"
      via: "renameio.Symlink call"
      pattern: "renameio\\.Symlink"
    - from: "internal/fs/protected_test.go"
      to: "internal/fs/testdata/python_shared_paths.txt"
      via: "os.ReadFile fixture comparison"
      pattern: "ReadFile.*python_shared_paths"
---

<objective>
Implement the atomic symlink engine and protected-paths enforcement in internal/fs.

Purpose: These are the two safety-critical primitives that all profile operations depend on. AtomicSymlink guarantees no broken state during profile switches. IsProtected guarantees credentials and history are never touched.

Output: internal/fs/atomic.go, internal/fs/protected.go, fixture file, and comprehensive tests
</objective>

<execution_context>
@/home/matthew/.claude/get-shit-done/workflows/execute-plan.md
@/home/matthew/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- From Plan 01 — executor needs go.mod to exist so `go get` works -->
From go.mod (created by Plan 01):
```
module github.com/matthewfritsch/claudehopper
```

No imports from cmd/ needed — internal/fs is independent.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AtomicSymlink with renameio/v2</name>
  <files>internal/fs/atomic.go, internal/fs/atomic_test.go</files>
  <behavior>
    - Test 1: AtomicSymlink creates a new symlink — os.Readlink returns expected target
    - Test 2: AtomicSymlink replaces an existing symlink — os.Readlink returns new target, old target no longer referenced
    - Test 3: AtomicSymlink with non-existent target still creates symlink (dangling link is valid)
    - Test 4: AtomicSymlink target path is absolute — verifies link resolves correctly
  </behavior>
  <action>
    1. Create `internal/fs/atomic.go`:
       - `package fs`
       - Import `github.com/google/renameio/v2`
       - `func AtomicSymlink(targetPath, linkPath string) error` wrapping `renameio.Symlink(targetPath, linkPath)`
       - Doc comment explaining: atomically creates or replaces symlink, never leaves broken state, not supported on Windows
    2. Create `internal/fs/atomic_test.go` with tests per behavior block.
       - All tests use `t.TempDir()` for isolation
       - Use `os.Readlink` to verify symlink targets
       - Use `os.Lstat` (not os.Stat) to check symlink existence
    3. Run `go get github.com/google/renameio/v2@latest && go mod tidy`
    4. Verify tests pass.
  </action>
  <verify>
    <automated>go test ./internal/fs/ -run TestAtomicSymlink -v</automated>
  </verify>
  <done>
    - AtomicSymlink creates new symlinks verified by os.Readlink
    - AtomicSymlink replaces existing symlinks atomically
    - All tests pass with `go test ./internal/fs/ -v`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Protected paths with fixture verification</name>
  <files>internal/fs/protected.go, internal/fs/protected_test.go, internal/fs/testdata/python_shared_paths.txt</files>
  <behavior>
    - Test 1: IsProtected returns true for each of the 11 SHARED_PATHS entries
    - Test 2: IsProtected returns false for non-protected paths (e.g., "settings.json", "agents", "random.txt")
    - Test 3: Go sharedPaths map has exactly the same entries as python_shared_paths.txt fixture (no extras, no missing)
    - Test 4: IsProtected handles edge cases — empty string returns false, path with slashes returns false (bare names only)
  </behavior>
  <action>
    1. Create `internal/fs/testdata/python_shared_paths.txt` with one path per line, matching SHARED_PATHS exactly:
       ```
       .credentials.json
       history.jsonl
       projects
       cache
       downloads
       transcripts
       shell-snapshots
       file-history
       backups
       session-env
       .session-stats.json
       ```
    2. Create `internal/fs/protected.go`:
       - `package fs`
       - `var sharedPaths = map[string]struct{}{...}` with all 11 entries matching the fixture
       - `func IsProtected(name string) bool` — looks up name in sharedPaths map
       - `func ProtectedPaths() []string` — returns sorted slice of all protected path names (useful for display/debugging in later phases)
       - Doc comment on sharedPaths noting it is the protected paths set, verified by fixture test
    3. Create `internal/fs/protected_test.go` with tests per behavior block.
       - TestIsProtected_MatchesPythonConstants reads testdata/python_shared_paths.txt, parses lines, verifies exact bidirectional match with Go map
       - TestIsProtected_True tests each protected path individually
       - TestIsProtected_False tests non-protected paths
       - TestIsProtected_EdgeCases tests empty string, paths with slashes
    4. Verify all tests pass.
  </action>
  <verify>
    <automated>go test ./internal/fs/ -run TestIsProtected -v</automated>
  </verify>
  <done>
    - IsProtected returns true for all 11 SHARED_PATHS entries
    - IsProtected returns false for non-protected paths
    - Fixture test confirms exact bidirectional match between Go constants and the fixture
    - All tests pass
  </done>
</task>

</tasks>

<verification>
- `go test ./internal/fs/ -v` — all tests pass
- `go vet ./internal/fs/` — no issues
- Fixture file contains exactly 11 lines matching SHARED_PATHS
</verification>

<success_criteria>
AtomicSymlink safely creates and replaces symlinks without broken state. IsProtected correctly identifies all 11 protected paths and is verified against a fixture to prevent drift.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
