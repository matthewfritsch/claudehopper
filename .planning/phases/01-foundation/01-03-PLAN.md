---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - internal/config/paths.go
  - internal/config/paths_test.go
  - internal/config/config.go
  - internal/config/config_test.go
  - internal/config/manifest.go
  - internal/config/manifest_test.go
  - internal/config/testdata/config.json
  - internal/config/testdata/hop-manifest.json
autonomous: true
requirements: [SAFE-03]

must_haves:
  truths:
    - "ConfigDir returns path under XDG_CONFIG_HOME when set"
    - "ConfigDir returns path under $HOME/.config when XDG_CONFIG_HOME is not set"
    - "ConfigDir never returns a path containing tilde (~)"
    - "config.json round-trips with exact same keys and values as Python version"
    - "hop-manifest.json round-trips with managed_paths (sorted), shared_paths, and description matching Python format"
  artifacts:
    - path: "internal/config/paths.go"
      provides: "ConfigDir and ProfilesDir path resolution"
      exports: ["ConfigDir", "ProfilesDir"]
      min_lines: 15
    - path: "internal/config/config.go"
      provides: "Config struct and Load/Save for config.json"
      exports: ["Config", "LoadConfig", "SaveConfig"]
      min_lines: 25
    - path: "internal/config/manifest.go"
      provides: "Manifest struct and Load/Save for .hop-manifest.json"
      exports: ["Manifest", "LoadManifest", "SaveManifest"]
      min_lines: 30
    - path: "internal/config/testdata/config.json"
      provides: "Python-compatible config.json fixture"
      contains: "active"
    - path: "internal/config/testdata/hop-manifest.json"
      provides: "Python-compatible manifest fixture"
      contains: "managed_paths"
  key_links:
    - from: "internal/config/paths.go"
      to: "os.UserConfigDir"
      via: "stdlib call for XDG resolution"
      pattern: "os\\.UserConfigDir"
    - from: "internal/config/config.go"
      to: "internal/config/paths.go"
      via: "ConfigDir call for default path"
      pattern: "ConfigDir"
    - from: "internal/config/manifest.go"
      to: "encoding/json"
      via: "JSON marshal/unmarshal with sorted managed_paths"
      pattern: "json\\.Marshal|json\\.Unmarshal"
---

<objective>
Implement config path resolution and Python-compatible JSON serialization for config.json and .hop-manifest.json.

Purpose: All profile operations read and write these JSON files. Format compatibility with the Python version is a hard constraint — a manifest written by Python must be readable by Go and vice versa. Path resolution must respect XDG_CONFIG_HOME and never store tilde strings.

Output: internal/config/ package with paths, config, and manifest modules plus fixture tests
</objective>

<execution_context>
@/home/matthew/.claude/get-shit-done/workflows/execute-plan.md
@/home/matthew/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- From Plan 01 — executor needs go.mod to exist so imports work -->
From go.mod (created by Plan 01):
```
module github.com/matthewfritsch/claudehopper
```

No imports from cmd/ or internal/fs/ needed — internal/config is independent.

<!-- Python-compatible JSON formats from RESEARCH.md -->
config.json format:
```json
{"active": "gsd"}
```

.hop-manifest.json format:
```json
{
  "managed_paths": ["agents", "settings.json"],
  "shared_paths": {},
  "description": "get-shit-done spec-driven development"
}
```
Note: managed_paths is a sorted JSON array. shared_paths is an object mapping filename to source profile name. description is a plain string.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Config path resolution with XDG support</name>
  <files>internal/config/paths.go, internal/config/paths_test.go</files>
  <behavior>
    - Test 1: ConfigDir returns filepath.Join(XDG_CONFIG_HOME, "claudehopper") when XDG_CONFIG_HOME is set
    - Test 2: ConfigDir returns filepath.Join(home, ".config", "claudehopper") when XDG_CONFIG_HOME is not set (uses os.UserConfigDir default)
    - Test 3: ConfigDir result never contains "~" character
    - Test 4: ProfilesDir returns ConfigDir + "/profiles"
    - Test 5: ConfigDir result is an absolute path (starts with "/")
  </behavior>
  <action>
    1. Create `internal/config/paths.go`:
       - `package config`
       - `func ConfigDir() (string, error)` — calls `os.UserConfigDir()`, returns `filepath.Join(base, "claudehopper")`, wraps error with context
       - `func ProfilesDir() (string, error)` — calls ConfigDir, returns `filepath.Join(cfg, "profiles")`
       - `func ProfileDir(name string) (string, error)` — calls ProfilesDir, returns `filepath.Join(profiles, name)` (convenience for Phase 2)
       - `func ConfigFilePath() (string, error)` — returns `filepath.Join(ConfigDir(), "config.json")`
    2. Create `internal/config/paths_test.go` with tests per behavior block.
       - Use `t.Setenv("XDG_CONFIG_HOME", t.TempDir())` for XDG override tests
       - Use `strings.Contains(result, "~")` check to ensure no tilde
       - Use `filepath.IsAbs(result)` to ensure absolute path
    3. Verify tests pass.
  </action>
  <verify>
    <automated>go test ./internal/config/ -run TestConfigDir -v && go test ./internal/config/ -run TestProfilesDir -v</automated>
  </verify>
  <done>
    - ConfigDir respects XDG_CONFIG_HOME override
    - ConfigDir returns absolute path with no tilde
    - ProfilesDir appends /profiles correctly
    - All path tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Python-compatible config.json and manifest JSON serialization</name>
  <files>internal/config/config.go, internal/config/config_test.go, internal/config/manifest.go, internal/config/manifest_test.go, internal/config/testdata/config.json, internal/config/testdata/hop-manifest.json</files>
  <behavior>
    config.json:
    - Test 1: LoadConfig reads {"active": "gsd"} and returns Config with Active="gsd"
    - Test 2: SaveConfig writes Config{Active: "gsd"} and produces {"active": "gsd"} with 2-space indent + trailing newline
    - Test 3: LoadConfig from fixture file testdata/config.json round-trips identically (byte-equal after save)
    - Test 4: LoadConfig returns zero Config (Active="") for missing file

    manifest:
    - Test 5: LoadManifest reads fixture with managed_paths, shared_paths, description
    - Test 6: SaveManifest writes with managed_paths sorted alphabetically
    - Test 7: SaveManifest produces 2-space indented JSON with trailing newline
    - Test 8: Manifest with empty shared_paths serializes as {} not null
    - Test 9: Manifest with empty managed_paths serializes as [] not null
    - Test 10: LoadManifest from testdata/hop-manifest.json round-trips identically
  </behavior>
  <action>
    1. Create fixture files:
       - `internal/config/testdata/config.json`: `{"active": "gsd"}` with trailing newline, 2-space indent matching Python output
       - `internal/config/testdata/hop-manifest.json`: Copy exact Python format:
         ```json
         {
           "managed_paths": [
             "agents",
             "settings.json"
           ],
           "shared_paths": {},
           "description": "get-shit-done spec-driven development"
         }
         ```
         with trailing newline
    2. Create `internal/config/config.go`:
       - `type Config struct` with `Active string` json:"active"`
       - `func LoadConfig(path string) (Config, error)` — reads file, json.Unmarshal, returns zero Config if file not found
       - `func SaveConfig(path string, cfg Config) error` — json.MarshalIndent with "", "  " prefix/indent, append newline, write atomically (write to tmp + rename or use os.WriteFile with 0644)
    3. Create `internal/config/manifest.go`:
       - `type Manifest struct` with:
         - `ManagedPaths []string` json:"managed_paths"` — use custom MarshalJSON or sort before save to ensure sorted output
         - `SharedPaths map[string]string` json:"shared_paths"` — ensure empty map serializes as {} not null
         - `Description string` json:"description"`
       - `func LoadManifest(path string) (Manifest, error)` — reads file, json.Unmarshal
       - `func SaveManifest(path string, m Manifest) error` — sort ManagedPaths, ensure SharedPaths is non-nil (empty map not nil), json.MarshalIndent, append newline, write file
       - `func NewManifest(description string) Manifest` — returns Manifest with empty non-nil slices and maps
    4. Create test files with tests per behavior block.
       - Round-trip tests: load fixture, save to temp, compare bytes
       - Nil-safety tests: empty slices/maps serialize as [] and {} not null
    5. Verify all tests pass.
  </action>
  <verify>
    <automated>go test ./internal/config/ -v</automated>
  </verify>
  <done>
    - config.json loads and saves in Python-compatible format
    - .hop-manifest.json loads and saves with sorted managed_paths, non-null empty collections
    - Fixture round-trip tests confirm byte-level compatibility
    - All tests pass
  </done>
</task>

</tasks>

<verification>
- `go test ./internal/config/ -v` — all tests pass
- `go vet ./internal/config/` — no issues
- Fixture files match Python output format exactly
</verification>

<success_criteria>
Config path resolution respects XDG_CONFIG_HOME with no tilde strings. config.json and .hop-manifest.json serialize in exact Python-compatible format, verified by fixture round-trip tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
